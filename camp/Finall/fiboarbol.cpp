#include<bits/stdc++.h>
using namespace std;
#define debug(x) cerr << #x << " = " << (x) << endl;

int main(){
    int n;
    cin>>n;
    while(n--){
        int a,b;
        cin>>a>>b;
        int d=abs(a-b);
    }
    return 0;
}